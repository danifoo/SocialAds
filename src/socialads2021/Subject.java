package socialads2021;

public interface Subject {
	public boolean notify(Advertisement advertisement);
	
	/**
	 * The attach and detach methods have been left in the code as a comment
	 * to show that they had previously been implemented but removed later as they server no practical purpose. 
	 */
	
	//public void attach(Observer observer);
	//public void detach(Observer observer);
}
